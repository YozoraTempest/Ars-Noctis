# Ars-Noctis 协作规范

## 适用范围

本仓库用于维护相互独立的 Agent Skills。每项 Skill 放在 `skills/<skill-name>/`，不得把多个不相关工作流合并成一个万能 Skill。

## 工作原则

- 修改前阅读本文件及目标目录下更近的 `AGENTS.md`。
- 先用具体使用示例确认触发条件、职责、非目标和升级边界，再创建或扩展 Skill。
- 保持渐进式披露：元数据负责触发，`SKILL.md` 负责核心流程，详细资料按需放入 `references/`。
- 编排扩展通过同级 `noctis.yaml` 声明，不为发现能力读取其他 Skill 的正文；项目状态通过脚本维护，不手工改写结构化标记。
- 默认相信代理具备通用工程能力，只记录稳定、非显而易见且能改变执行结果的知识。
- 不为尚未出现的需求预建脚本、目录、模板或兼容层。
- 保留工作树中已有且与当前任务无关的修改。

## 变更与验证

- 新建 Skill 使用当前环境提供的官方 Skill 初始化工具，不手工仿造骨架。
- 修改后运行官方快速校验；包含脚本时还需实际执行代表性用例。
- 对触发或流程较复杂的 Skill，使用不泄露预期答案的真实请求做正向验证。
- 未经明确要求，不提交、不推送、不发布，也不修改远程仓库状态。

## Git

- 提交信息使用 Conventional Commits，摘要使用中文。
- 一个提交只覆盖一个 Skill、一个共享工具链阶段或一个仓库级关注点。
- 提交前检查暂存差异，并报告实际执行的验证。
