interface:
  display_name: "Noctis Continue"
  short_description: "扫描并选择 Noctis 的未完成工作流与执行入口"
  default_prompt: "使用 $noctis-continue 扫描现有状态机，选择 active、ready 或 blocked 入口并交给 Exec。"

policy:
  allow_implicit_invocation: false
