interface:
  display_name: "Noctis Exec"
  short_description: "物化 Noctis 计划并通过统一入口推进执行生命周期"
  default_prompt: "使用 $noctis-exec 物化已确认计划，或接收统一 ExecutionEntry 推进执行生命周期。"

policy:
  allow_implicit_invocation: false
