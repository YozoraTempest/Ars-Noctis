interface:
  display_name: "Noctis"
  short_description: "编排原生 Ars、Artifact 与 Task 依赖并生成执行计划"
  default_prompt: "使用 $noctis 选择最小工作层级，确认 Ars、Artifact、依赖与授权，并生成 ExecutionPlan。"

policy:
  allow_implicit_invocation: false
