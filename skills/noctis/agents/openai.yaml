interface:
  display_name: "Noctis"
  short_description: "规划 Task、Unit 或 Work，并生成已确认的执行计划"
  default_prompt: "使用 $noctis 选择最小工作层级，确认依赖与授权，并生成 ExecutionPlan。"

policy:
  allow_implicit_invocation: false
