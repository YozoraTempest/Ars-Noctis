version: 1
kind: capability
id: verify
contract: 1
summary: 执行用户确认的实际行为场景并记录可观察证据与判定。
entry:
  task_status: active
  capability: verify
artifacts:
  inputs:
    review:
      type: review-record
      formats:
        - ars.review@1
      required: false
    reviews:
      type: review-record
      formats:
        - ars.review@1
      required: false
      cardinality: many
  outputs:
    verification:
      type: verification-record
      formats:
        - ars.verification@1
      required: true
outcomes:
  - completed
  - spliced
  - waiting
  - deferred
  - blocked
allows:
  - confirmed-environment-action
  - verification-record-write
  - local-evidence
  - orchestration-write
  - local-checkpoint
forbids:
  - unconfirmed-external-write
  - project-write
  - static-code-review
  - push
  - deploy
